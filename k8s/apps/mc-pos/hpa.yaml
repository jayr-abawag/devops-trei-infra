apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: pos-backend-hpa
  namespace: mc-pos
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pos-backend
  minReplicas: 2
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: pos-frontend-hpa
  namespace: mc-pos
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: pos-frontend
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
